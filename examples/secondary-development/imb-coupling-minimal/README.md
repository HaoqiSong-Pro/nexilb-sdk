# imb-coupling-minimal

Contract-only skeleton for the contact-free IMB portion of
`model.NPhaseImbDemContactAngle`. No particle values, numerical input,
reference, tolerance, digest, device requirement, execution, or result is
asserted. The approved fixture must contain one dynamic particle that remains
strictly separated from boundaries and all other particles throughout the
observation window; every accepted snapshot must report zero contacts.
